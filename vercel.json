{
    "github": {
        "silent": true
    },
    "buildCommand": "npm install && npm run build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}